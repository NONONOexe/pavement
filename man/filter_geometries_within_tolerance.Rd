% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter-points-within-tolerance.R
\name{filter_geometries_within_tolerance}
\alias{filter_geometries_within_tolerance}
\title{Filter geometries within a tolerance distance}
\usage{
filter_geometries_within_tolerance(
  geometries,
  reference_geometry,
  tolerance = 0.01
)
}
\arguments{
\item{geometries}{A \code{sfc} object containing geometries to filter.}

\item{reference_geometry}{A single geometry object to use as a reference.}

\item{tolerance}{The maximum distance allowed between geometries and
the reference geometry. Geometries within this distance from
the reference geometry will be kept in the result.}
}
\value{
A \code{sfc} object containing the filtered geometries.
}
\description{
This function filters geometries that are within a specified tolerance
distance from a reference geometry.
}
\examples{
library(sf)

# Create a points
points <- st_sfc(
  st_point(c(0.000, 0.000)),
  st_point(c(1.000, 0.100)),
  st_point(c(2.000, 0.010)),
  st_point(c(4.000, 0.001)),
  st_point(c(5.001, 0.000)),
  st_point(c(6.000, 0.000))
)

# Create a linestring (reference geometry)
linestring <- create_linestring(0, 0, 5, 0)

# Plot the points
plot(linestring, col = "gray")
plot(points, add = TRUE)

# Filter points within a tolerance distance (default: 0.01)
filtered_points <- filter_geometries_within_tolerance(points, linestring)

# Plot the filtered points
plot(linestring, col = "gray")
plot(filtered_points, add = TRUE)
}
