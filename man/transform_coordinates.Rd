% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform-coordinates.R
\name{transform_coordinates}
\alias{transform_coordinates}
\alias{transform_to_cartesian}
\alias{transform_to_geographic}
\title{Transform to Cartesian or geographic coordinates}
\usage{
transform_coordinates(
  spatial_object,
  target = c("cartesian", "geographic"),
  quiet = FALSE
)

transform_to_cartesian(spatial_object, quiet = FALSE)

transform_to_geographic(spatial_object, quiet = FALSE)
}
\arguments{
\item{spatial_object}{A spatial object.}

\item{target}{A character string specifying the target coordinate system:
\code{"cartesian"} (EPSG:6675) or \code{"geographic"} (EPSG:4326).}

\item{quiet}{Logical. If \code{TRUE}, suppresses the warning when CRS is missing.}
}
\value{
A spatial object transformed to the specified coordinate system.
}
\description{
This function transforms a spatial object to Cartesian coordinates (plane
rectangular coordinate system) or geographic coordinates (WGS84; EPSG:4326).
The Cartesian system currently supports only Aichi Prefecture's JDG2011
system (EPSG:6675).
}
\details{
If the CRS is missing (NA), a warning is issued, and the original object is
returned. The warning can be suppressed with the \code{quiet} argument.
}
\examples{
# Create points
points <- create_points(136.9024, 35.1649, crs = 4326)
points

# Transform to Cartesian coordinates
transformed <- transform_to_cartesian(points)
transformed

# Transform to geographic coordinates
transformed <- transform_to_geographic(points)
transformed
}
